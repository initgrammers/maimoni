import { describe, expect, test } from 'bun:test';

describe('claimAnonymousData integration (bun)', () => {
  test('run `bun run test:integration` to execute the Node/Vitest integration suite', () => {
    expect(true).toBe(true);
  });
});
